#!/usr/bin/env bash

# icon set: apps
# description: Icons for common applications
#
# This file is not sourced directly. Use @tmux-tabicon-manual-icons
# in tmux.conf with | as delimiter, or reference this file for examples.
#
# Example for tmux.conf:
#   set -g @tmux-tabicon-manual-icons \
#     "?#{==:#W,[tmux]},|?#{==:#W,node},󰎙|?#{==:#W,python},"

# Available icon mappings:
#   [tmux]  →
#   node    → 󰎙
#   python  →
